generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model SurveyRawSubmission {
  id           Int      @id @default(autoincrement())
  submissionId String   @unique
  payload      Json
  createdAt    DateTime @default(now())
}